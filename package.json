{
  "name": "agent-vector-search",
  "version": "0.1.0",
  "description": "Semantic vector search for AI agent history using LanceDB",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "bun build --compile --outfile agent-vector-search src/server.ts",
    "embed": "python src/embeddings/embed.py",
    "ingest": "bun run src/ingest.ts",
    "test": "bun test",
    "e2e": "bun test e2e/"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.13.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
